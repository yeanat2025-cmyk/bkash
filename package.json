{
  "name": "send-money-api",
  "version": "1.0.0",
  "description": "Simple JSON API for Send Money App",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
